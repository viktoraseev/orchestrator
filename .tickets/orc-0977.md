---
id: orc-0977
status: closed
deps: []
links: []
created: 2026-09-04T04:59:12Z
type: chore
priority: 1
assignee: viktoraseev
tags: [documentation, gherkin, contract, migration]
---
# Перенести следующие 20 нормативных правил из Markdown в Gherkin

Четвёртая партия устранения дублирования продуктового контракта: выбрать следующие 20 наблюдаемых правил из SPEC.md, workflow.spec.md, format.spec.md и cli.md, закрепить их в самодостаточных acceptance-сценариях и удалить только соответствующие Markdown-дубли.

## Acceptance Criteria

Ровно 20 правил перечислены в истории тикета; каждое явно наблюдаемо в features/*.feature; соответствующие дубли удалены из Markdown без потери соседних гарантий; source tags и шаги остаются целостными; ./scripts/test.sh, ./scripts/build.sh и git diff --check успешны.


## Notes

**2026-09-04T05:01:09Z**

Выбраны 20 правил read-only inspection для batch 4:\n1) inspection не получает Run lock, не создаёт control endpoint/executor и не меняет files;\n2) занятый supervisor'ом run доступен для чтения;\n3) run list игнорирует нечисловые entries;\n4) каждый числовой run проходит полную durable validation;\n5) runs сортируются по возрастанию RunId;\n6) text list печатает по одной точной summary-строке;\n7) отсутствие run/ или runs даёт успешный пустой вывод;\n8) повторяемые --state объединяются как OR;\n9) --workflow соединяется со state как AND;\n10) одинаковые filters идемпотентны;\n11) все runs валидируются до фильтрации;\n12) невалидные state/format/WorkflowId дают 2 до чтения runs;\n13) active означает unfinished attempt либо ready frontier;\n14) completed означает завершённую модель без frontier;\n15) остальные валидные модели являются blocked;\n16) run show имеет фиксированный text order и форму;\n17) run artifacts выводит только completed artifacts в deterministic order;\n18) run artifact выбирает exact completed version и копирует bytes без преобразования;\n19) text является default, а JSON сохраняет snake_case typed model, numbers/null/arrays/order;\n20) run verify --all идёт по RunId, продолжает после invalid runs и возвращает 3 только после полного JSON report.\nУдаляются только соответствующие clauses из SPEC.md и cli.md; fingerprint retry, watch, selected verify, I/O и остальные ещё не перенесённые гарантии остаются.

**2026-09-04T05:05:04Z**

Завершено: 20 правил read-only inspection закреплены в features/run_inspection.feature и удалены из дублирующих clauses SPEC.md/cli.md. Добавлены process-сценарии для non-numeric entries, full validation до фильтрации, OR/AND/idempotent filters, ранних syntax errors, пустого artifacts list, nullable JSON и полного ordered verify report; fixture теперь явно содержит unfinished и temporary artifacts. Неперенесённые fingerprint retry, watch, selected verify и I/O guarantees сохранены. Проверки: cargo test --test run_inspection — 22/22 scenarios, 98/98 steps; ./scripts/test.sh — exit 0; ./scripts/build.sh — exit 0; git diff --check — exit 0; residual search удалённых формулировок пуст.
